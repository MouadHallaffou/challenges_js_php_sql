Docker               | La plateforme
Docker Engine        | Le moteur d’exécution
Docker CLI           | L’interface pour écrire les commandes
Dockerfile           | Fichier pour créer une image d’un service
docker-compose.yml   | Fichier pour gérer plusieurs services (containers)

Docker Container (حاوية دوكر) :
👉 حاوية تحتوي على التطبيق وكل متطلباته، معزولة عن بقية النظام.

Docker Compose (دوكر كومبوز) :
👉 أداة لتشغيل عدة حاويات معًا بسهولة باستخدام ملف YAML واحد.

Dockerfile (ملف دوكر) :
👉 ملف نصي يحدد كيفية بناء صورة دوكر خطوة بخطوة.

Namespaces Docker (مساحات الأسماء في دوكر) :
👉 تقنية تعزل الموارد (مثل العمليات، الملفات، الشبكة) لكل حاوية حتى لا تتداخل مع بعضها البعض.

Registre d'images Docker (سجل صور دوكر) :
👉 خادم يتم فيه تخزين ومشاركة صور دوكر.

Docker Hub (دوكر هاب) :
👉 السجل الرسمي العام لدوكر، يمكنك أن تجد أو ترفع فيه صور دوكر جاهزة.

Virtualisation vs Conteneurisation (الافتراضية مقابل الحاويات) :
👉 الافتراضية = إنشاء أنظمة تشغيل كاملة لكل تطبيق.
👉 الحاويات = مشاركة نفس نظام التشغيل لكن مع عزل التطبيقات عن بعضها البعض.

Hyperviseur (مشرف افتراضي) :
👉 برنامج يدير الآلات الافتراضية (مثل VirtualBox أو VMware).

Docker Daemon (خدمة دوكر) :
👉 البرنامج الذي يعمل في الخلفية ويدير تشغيل الحاويات.
